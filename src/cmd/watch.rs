#![allow(unused_variables)]

use crate::cli::WatchArgs;
use crate::cmd::Ctx;

/// Live TUI over a run journal.
pub async fn run(ctx: &Ctx, args: &WatchArgs) -> anyhow::Result<i32> {
    todo!("WP7")
}
