#![allow(unused_variables)]

use crate::cli::AdoptArgs;
use crate::cmd::Ctx;

/// Land a worker's work in the user's checkout.
pub async fn run(ctx: &Ctx, args: &AdoptArgs) -> anyhow::Result<i32> {
    todo!("WP7")
}
